mkdir -p ~/local
cd ~/local
package=LibreOffice_6.4.7.2_Linux_x86-64_rpm
echo -h " Do you need to download the pac"
read answer

#
if ! [ -e ${package}.tar.gz ] || ! [ -e ${package} ]
then
    wget https://downloadarchive.documentfoundation.org/libreoffice/old/6.4.7.2/rpm/x86_64/${package}.tar.gz
else
    echo " Not downloading again. Using existing file: ${package}.tar.gz"
fi
tar -xvf ${package}.tar.gz
cd ${package}
sudo dnf install -y RPMS/*.rpm
sudo dnf install -y java-latest-openjdk
cd -
